# =============================================================================
# 실시간 대시보드(dashboard.py) 실행용 의존성 — Streamlit Cloud 배포 대상
# =============================================================================
# 전체 백엔드 플랫폼(FastAPI·Celery·DB 등) 의존성은 requirements-full.txt 참고.
# =============================================================================

# ── 대시보드 / 시각화 ──
streamlit>=1.40
plotly>=5.24

# ── 데이터 ──
yfinance>=0.2.50
pandas>=2.2
numpy>=1.26,<2.3

# ── 가치·ML 분석 페이지 ──
scipy>=1.13
scikit-learn>=1.5
xgboost>=2.1
lightgbm>=4.5
hmmlearn>=0.3.2
